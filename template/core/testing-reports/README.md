Local home for validation evidence and failure diagnostics (the `/qa`, `/security`,
`/perf` gates and script stubs write artifacts here). Success needs no report — on
failure, keep enough evidence to fix without re-running. Never committed: the
artifacts/ subdir is gitignored.
